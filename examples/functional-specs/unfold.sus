//  if (start == 0) {
//    let m = malloc(2);
//    *m = start;
//    let rest = malloc(1);
//    *(m+1) = rest;
//    unfoldList(rest, start-1);
//    *x = m;
//  } else {
//    let m = malloc(1);
//    *m = start;
//    *x = m;
//  }

void unfoldList(loc x)
  { x :-> y ** unconstrained(y, preS) }
  { x :-> y ** fill(y, postS, 7) }
{
  ??
}

###
