void mapTimes2(loc x, loc y, loc intermediateArg)
  { x :-> w1 ** unconstrained(w1, preS) }
  { x :-> w1 ** constrained_list(w1, preS, pred(v, r) => r == 2*v) }
{
  let w = *x;
  if (w == 0) {
  } else {
    ??
  }
}

###

